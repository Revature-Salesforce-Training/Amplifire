({
    doInit: function(component, event, helper) {
        console.log('DoinIt');
    },
    
    handleLeadButton: function(component, event, helper) {
        helper.handleLeadButton(component, event);
    }
})
